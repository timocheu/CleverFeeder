# Clever Feeder
ESP32 Based cat feeder.

## Description
A feeder that has multiple sensors for detected the cat in vicinity.
It can calibrate the weight and give food automatically.

## Setup
make a "config.h" file in src for setting up WiFi
```
#define SSID "YourSSID"
#define PASSWORD "YourPASSWORD"
```
